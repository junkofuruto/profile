services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

  nginx:
    build: .
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - frontend
      - backend
